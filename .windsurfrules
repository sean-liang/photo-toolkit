1. Only english is allowed in scripts, including logs, print statements, and comments.
2. Use print() for logging and debugging.
3. Use tqdm for progress bar. Inside tqdm block, use tqdm.write() for print statements and tqdm.set_postfix() for updating the progress display.